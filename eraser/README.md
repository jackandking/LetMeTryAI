# 爱橡皮 (Love Eraser)

汉字练习助手 - 帮助生成可打印的田字格练习模板

## 功能说明

这个工具允许用户上传包含田字格和汉字的图片，然后生成一个擦除了汉字但保留了拼音和田字格线条的版本，供孩子练习写字使用。

## 当前实现状态

### ✅ 已完成功能
- 图片上传（支持拖拽和点击上传）
- 文件类型验证（JPG/PNG）
- 文件大小验证（最大10MB）
- 响应式UI设计
- **客户端图像处理**：使用Canvas API实现
  - 自动检测田字格结构
  - 智能擦除汉字区域
  - 保留拼音和网格线
- 处理后图片下载功能
- **网页调试功能**：集成VConsole移动端调试工具（v3.15.1）
  - 访问页面时添加 `?debug=true` 参数即可启用
  - 示例：`/eraser?debug=true`
  - 按需加载，不影响正常使用性能

### 🚧 待完善功能
- **PDF导出**：集成jsPDF库实现
- **批量处理**：支持一次上传多张图片
- **参数可调**：允许用户调整擦除强度和识别灵敏度
- **机器学习增强**：集成TensorFlow.js进行更精确的字符识别（可选）

## 技术实现

### 依赖
- `util/file-util.js` - 文件上传工具
- `config.js` - 全局配置
- Canvas API - 客户端图像处理

### 图像处理算法
应用使用纯JavaScript实现的改进图像处理算法：

#### 算法改进 (2025年12月更新)
1. **自适应阈值**：使用Otsu算法自动计算最佳阈值，适应不同光照条件
2. **改进的网格检测**：基于连续性分析检测水平和垂直网格线
3. **智能拼音检测**：使用密度分析自动识别拼音区域（5-30%密度）
4. **背景色估计**：从邻近像素估计背景色，实现更自然的擦除效果

#### 处理流程
1. **亮度计算**：计算每个像素的亮度值（使用标准公式：0.299R + 0.587G + 0.114B）
2. **自适应阈值**：使用Otsu方法自动计算最优阈值
3. **网格线检测**：识别跨越图像的长直线作为网格线
4. **拼音区域检测**：分析文本密度识别拼音区域
5. **字符擦除**：擦除未标记保留的暗色区域
6. **背景融合**：使用邻近背景色填充擦除区域

### 文件结构
```
eraser/
├── index.html      # 主页面
├── app.js          # 应用逻辑
├── styles.css      # 样式
├── app.test.js     # 单元测试
└── README.md       # 本文档
```

## 使用说明

1. 打开 `/eraser` 页面
2. 点击或拖拽上传田字格汉字图片
3. 点击"开始处理"
4. 等待客户端处理（通常1-2秒）
5. 下载处理后的图片

**提示**：
- 使用清晰、光线充足的图片效果最佳
- 建议图片中田字格结构规整
- 处理完全在浏览器中进行，无需上传到服务器处理

### 调试模式

如需在移动设备上调试应用，可以启用VConsole：
1. 访问 `/eraser?debug=true`
2. 页面加载后会自动显示VConsole调试面板
3. 可以查看console日志、网络请求、元素信息等

**注意**：正式使用时请勿携带 `debug=true` 参数，以获得最佳性能。

## 未来改进方向

1. **提升处理质量**
   - 集成机器学习模型（如TensorFlow.js）进行更精确的字符识别
   - 支持不规则网格和多种书写格式
   - 自适应阈值调整

2. **增强用户体验**
   - 进度条显示处理进度
   - 预览对比（原图 vs 处理后）
   - 可调整参数（擦除强度、识别灵敏度）
   - 实时预览效果

3. **扩展功能**
   - 支持生成空白田字格模板
   - 支持自定义汉字和拼音
   - 打印设置和布局优化
   - 批量处理多张图片

## 测试

运行单元测试：
```bash
npm test -- eraser/app.test.js
```

## 许可证

MIT License - 与项目主许可证保持一致
